/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  quoteProps: 'consistent',
  trailingComma: 'none',
  bracketSameLine: true,
  printWidth: 120
}

module.exports = config
